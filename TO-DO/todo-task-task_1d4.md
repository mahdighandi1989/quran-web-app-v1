# TODO — Task task_1d4 (نیاز به تکمیل دستی)

> **پیاده‌سازی تشخیص گفتار و بارگذاری داینامیک XLSX**

## 🔎 خلاصه وضعیت

- **task_id**: `task_1d405c6321cf`
- **repo**: `mahdighandi1989/quran-web-app-v1`
- **verification_status**: `partial`
- **archived_reason**: `max_retries` — Claude به سقف retry رسید بدون اینکه verify=done شود
- **retries_done**: 2
- **verifier confidence**: 0.92
- **verifier model**: `—`
- **report_id**: `6cc32c2b-d610-4c6c-a5fb-e9bd6dba86e3`
- **created_at**: 2026-06-05T05:23:41.955867+00:00

## 🚧 چه چیزی باقی مانده (مهم‌ترین بخش)

- [ ] حداقل 10 تست واحد برای همه توابع ابزاری (segGraphemes, eq, getSimilarity, isAllGreen) وجود ندارد
- [ ] تست‌های احراز هویت Firebase با mock کردن firebase/auth پیاده‌سازی نشده
- [ ] تست‌های همگام‌سازی Google Drive با mock کردن fetch پیاده‌سازی نشده
- [ ] تمام صفحات قابل مرور نیستند (route /quran/page/1 404 می‌دهد)
- [ ] فایل App.jsx کمتر از 200 خط نشده (بیش از 3500 خط است)
- [ ] ماژول‌های جدید حداکثر 200 خط ندارند (برخی فایل‌ها بیش از 200 خط)
- [ ] react-router-dom به package.json اضافه نشده
- [ ] حداقل 4 مسیر (/, /practice, /hifz, /settings) تعریف نشده
- [ ] URL با navigation به روز نمی‌شود (مسیریابی پیاده‌سازی نشده)
- [ ] قابلیت بازگشت با دکمه Back مرورگر کار نمی‌کند
- [ ] برنامه build و run می‌شود اما مسیریابی کار نمی‌کند

## 👉 قدم‌های بعدی پیشنهادی (از verifier)

1. نصب react-router-dom و تعریف مسیرهای اصلی در App.jsx
2. تقسیم App.jsx به ماژول‌های مجزا با حداکثر 200 خط
3. افزودن تست‌های Firebase auth و Google Drive sync با mock
4. تکمیل تست‌های واحد برای segGraphemes, eq, getSimilarity, isAllGreen
5. رفع 404 برای route /quran/page/N

## ✅ چه چیزی Claude انجام داد

- [x] فایل src/App.test.jsx ایجاد شده (src/main.test.jsx معادل)
- [x] تست‌های واحد برای normAR و levenshtein پیاده‌سازی شده
- [x] تمامی تست‌ها با npm test عبور می‌کنند
- [x] فایل src/data/quranPages.json شامل 604 صفحه قرآن است
- [x] هر صفحه شامل surahها و ayahهای صحیح است
- [x] برنامه بعد از جایگزینی بدون خطا کار می‌کند
- [x] لود داینامیک XLSX از CDN پیاده‌سازی شده
- [x] پردازش فایل اکسل بدون خطا انجام می‌شود
- [x] دکمه میکروفون در UI ظاهر شده و درخواست دسترسی به میکروفون می‌کند
- [x] پس از تشخیص گفتار متن در فیلد مربوطه نمایش داده می‌شود
- [x] ریشه anti-pattern تشخیص داده شده و کد اصلاح یا کامنت توجیهی اضافه شده
- [x] تست edge case برای anti-pattern نوشته شده
- [x] برنامه بدون خطا اجرا شده و تمام قابلیت‌ها کار می‌کنند
- [x] importهای جدید در App.jsx به درستی کار می‌کنند
- [x] ✓ تبدیل یادداشت مهم به مرحله اجرایی: بررسی وابستگی‌ها و همگام‌سازی چهارجهته برای توسعه هسته فرانت‌اند (code-aware: implemented)
- [x] ✓ افزودن تست واحد برای فایل App.jsx با پوشش منطق احراز هویت و همگام‌سازی (code-aware: implemented)
- [x] ✓ ایجاد تست‌های واحد برای توابع normAR و levenshtein (code-aware: implemented)
- [x] ✓ تکمیل ساختار 604 صفحه قرآن در src/data/quranPages.json (code-aware: implemented)
- [x] ✓ تکمیل ساختار صفحه‌بندی قرآن با تمام ۶۰۴ صفحه (code-aware: implemented)
- [x] ✓ ایجاد فایل داده کامل 604 صفحه قرآن (quranPages.json) (code-aware: implemented)
- [x] ✓ تکمیل داده صفحات قرآن با جایگزینی ثابت QURAN_PAGE_STRUCTURE_DEFAULT با فایل JSON شامل تمام 604 صفحه (code-aware: implemented)
- [x] ✓ پیاده‌سازی تشخیص گفتار و لود داینامیک XLSX در App.jsx (code-aware: implemented)
- [x] ✓ تبدیل یادداشت مهم به یک مرحله اجرایی: پیش‌نیازهای خواندن کامل، بررسی وابستگی‌ها و مدیریت TO-DO (code-aware: implemented)
- [x] ✓ پیاده‌سازی لود داینامیک XLSX از CDN و راه‌اندازی Web Speech API در App.jsx (code-aware: implemented)
- [x] ✓ پیاده‌سازی بارگذاری داینامیک XLSX و تشخیص گفتار با Web Speech API (code-aware: implemented)
- [x] ✓ لود داینامیک XLSX از CDN به جای بارگذاری همزمان (code-aware: implemented)
- [x] ✓ تغییر ساختار App.jsx از یک فایل ۱۳۲۶ خطی به یک orchestrator با ماژول‌های مجزا (code-aware: implemented)
- [x] ✓ بازنگری و رفع anti-pattern در هسته فرانت‌اند (App.jsx و مسیریابی) (code-aware: implemented)
- [x] ✓ اضافه کردن react-router-dom به package.json (code-aware: implemented)
- [x] ✓ اضافه کردن React Router به main.jsx با استفاده از BrowserRouter (code-aware: implemented)

## 📝 خلاصهٔ verifier

بخش عمده قابلیت‌های اصلی (تشخیص گفتار، لود داینامیک XLSX، داده‌های 604 صفحه قرآن، تست‌های پایه) پیاده‌سازی شده، اما معماری برنامه (مسیریابی، ماژولار کردن App.jsx، تست‌های جامع) هنوز ناقص است.

## 📋 Acceptance Criteria (مرجع کامل)

این لیست معیار done شدن تسک است — هر آیتمی که هنوز satisfy نیست
باید توسط انسان تکمیل شود.

- فایل src/App.test.jsx ایجاد شود
- حداقل 10 تست واحد برای توابع ابزاری (normAR, levenshtein, segGraphemes, eq, getSimilarity, isAllGreen) وجود داشته باشد
- تست‌های احراز هویت Firebase با mock کردن ماژول firebase/auth
- تست‌های همگام‌سازی Google Drive با mock کردن fetch
- تمامی تست‌ها با npm test عبور کنند
- فایل src/data/quranPages.json شامل تمام 604 صفحه قرآن باشد
- هر صفحه شامل surahها و ayahهای صحیح باشد
- برنامه بعد از جایگزینی بدون خطا کار کند
- تمام صفحات قابل مرور باشند
- با کلیک روی دکمه آپلود فایل اکسل، کتابخانه XLSX از CDN لود شود و پیام 'در حال بارگذاری' نمایش داده شود.
- پس از لود، فایل اکسل بدون خطا پردازش شود.
- دکمه میکروفون در رابط کاربری ظاهر شود و با کلیک، مرورگر درخواست دسترسی به میکروفون کند.
- پس از تشخیص گفتار، متن تشخیص داده شده در فیلد مربوطه نمایش داده شود.
- فایل App.jsx کمتر از 200 خط شود
- هر ماژول جدید حداکثر 200 خط داشته باشد
- برنامه بدون خطا اجرا شود و تمام قابلیت‌ها کار کنند
- importهای جدید در App.jsx به درستی کار کنند
- ریشه anti-pattern تشخیص داده شد
- یا کد اصلاح شد، یا کامنت توجیهی اضافه شد
- تست edge case نوشته شد
- react-router-dom در package.json اضافه شود
- حداقل 4 مسیر (routes) تعریف شود: /, /practice, /hifz, /settings
- URL با navigation به روز شود
- قابلیت بازگشت با دکمه Back مرورگر کار کند
- برنامه build و run شود

## 🔬 Evidence که verifier پیدا کرد

**Commits:**
- `a2ee914`
- `db571fc`
- `54a63c1`
- `0bc19af`
- `6e770df`
- `ce723ae`
- `dd8c5b1`

**Files lams شده:**
- `src/App.jsx`
- `src/main.test.jsx`
- `src/data/quranPages.json`
- `src/lib/arabic.js`
- `src/lib/excel.js`
- `src/lib/drive.js`
- `src/lib/firebase.js`

**Issues:**
- App.jsx بیش از 3500 خط
- react-router-dom نصب نشده
- مسیریابی پیاده‌سازی نشده

## 💡 ایدهٔ اصلی تسک

🧬 این یک تسک تلفیقی است — از 6 تسک منفرد ساخته شده.
📌 دلیل تلفیق (rationale توسط AI): این خوشه شامل تسک‌های مربوط به پیاده‌سازی قابلیت‌های جدید (تشخیص گفتار، لود XLSX، ساختار قرآن)، بازآرایی کد App.jsx، رفع اشکالات مربوط به الگوهای طراحی و اعتبارسنجی، و پیاده‌سازی React Router برای مدیریت مسیرها است. افزودن تست واحد برای App.jsx نیز به دلیل ماهیت حیاتی آن و ارتباط مستقیم با توسعه این بخش، در این خوشه قرار می‌گیرد.
🎯 theme: توسعه، بازآرایی و رفع اشکال هسته فرانت‌اند و مدیریت مسیرها
💎 estimated_difficulty: large

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 1 از 6
  id: abb2dab4-5cbd-4aad-ac19-c2c23afb3353
  عنوان اصلی: افزودن تست واحد برای منطق حیاتی App.jsx
  اولویت اصلی: critical
  وضعیت verify قبلی: partial
  فایل‌های دخیل: src/App.jsx

📋 acceptance_criteria کامل:
  - فایل src/App.test.jsx ایجاد شود [verify_method=static] [verify_plan={"grep_patterns": ["import { render, screen } from '@testing-library/react';", "import App from './App';", "describe('App', () => {"], "files_hint": ["src/App.test.jsx"]}]
  - حداقل 10 تست واحد برای توابع ابزاری (normAR, levenshtein, segGraphemes, eq, getSimilarity, isAllGreen) وجود داشته باشد [verify_method=static] [verify_plan={"grep_patterns": ["test('normAR", "test('levenshtein", "test('segGraphemes", "test('eq", "test('getSimilarity", "test('isAllGreen"], "files_hint": ["src/App.test.jsx"]}]
  - تست‌های احراز هویت Firebase با mock کردن ماژول firebase/auth [verify_method=backend_test] [verify_plan={"test_node": "src/App.test.jsx::test_firebase

## 📜 پرامپت اصلی (excerpt)

```
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  با

_[truncated — full prompt در پنل]_
```

---

_این فایل توسط Claude Auto-Runner تولید شده است. تسک با حالت_ `max_retries` _آرشیو شده و دیگر به‌صورت خودکار pickup نمی‌شود._